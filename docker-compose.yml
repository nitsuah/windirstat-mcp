services:
  windirstat-mcp:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: windirstat-mcp-server
    stdin_open: true
    volumes:
      - .:/app
      - C:/:/host-c:ro
    restart: "no"

networks:
  default:
    name: windirstat-mcp-net
networks:
  default:
    name: windirstat-mcp-net
